from typing import Annotated

from fastapi import Path

# Верхняя граница идентификатора — не украшение схемы. Без неё запрос к
# /courses/5790491620288948600832 проходит проверку (это целое и оно
# больше нуля), доезжает до базы и падает там на переполнении: снаружи
# это 500 на корректном с виду адресе. Нашёл контрактный прогон.
#
# Значение — максимум 32-битного целого: столько же занимает колонка
# идентификатора.
INT32_MAX = 2**31 - 1

ResourceId = Annotated[int, Path(ge=1, le=INT32_MAX)]
